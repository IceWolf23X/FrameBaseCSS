{
  "name": "framebasecss",
  "version": "1.1.0",
  "private": true,
  "description": "Maintainer tooling for FrameBaseCSS distribution assets.",
  "scripts": {
    "build:css": "node scripts/build-css.mjs",
    "check:css": "node scripts/build-css.mjs --check"
  },
  "devDependencies": {
    "clean-css": "5.3.3"
  },
  "engines": {
    "node": ">=18"
  }
}
